import React from 'react';
import ReactDOM from 'react-dom';
import VaaniApp from './containers/vaani';
import './index.scss';

// Render the app into the div with id `root`
ReactDOM.render(<VaaniApp />, document.getElementById('root'));